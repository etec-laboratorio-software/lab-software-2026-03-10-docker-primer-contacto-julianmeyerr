# Cómo ejecutar el proyecto

1. Ejecutar:
   docker compose up --build

2. Abrir en el navegador:
   http://localhost

Notas:
- Solo se expone el puerto 80
- El backend y la base de datos funcionan internamente con Docker